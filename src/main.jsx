import React from 'react'
import ReactDOM from 'react-dom/client'
import ProjectCatalog from './project-catalog.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ProjectCatalog />
  </React.StrictMode>,
)
